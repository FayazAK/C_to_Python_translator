#include<stdio.h>
int main()
  {
  int n,p,i,l,c=0;
     printf("enter the limit: ");
     scanf("%d",&n);
    for(p=2;p<=n;p++)
    {
     for(i=2;i<=p;i++)
        {
        if(p%i==0)
            c++;
         }
   if(c==1)
   {l=p;}
   c=0;
      }
    printf("largest prime =%d",l);
}
